[buildout]
# we default to extending the local config for development.  Change this when
# releasing to dev/maint/prod
extends =
    profiles/dev.cfg
#    profiles/prod.cfg

[versions]
lxml = 2.3
flake8 =
zc.buildout =
